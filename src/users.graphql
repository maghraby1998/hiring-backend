type User {
  id: ID
  name: String
}

type Query {
  users: [User]
}

input UserInput {
  name: String
  email: String
}

type Mutation {
  createUser(input: UserInput): User
  signIn(input: SignInInput): SignInResponse
}

input SignInInput {
  email: String
  password: String
}

type SignInResponse {
  user: User
  access_token: String
}
